/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.html"], // fichiers HTML où tailwind doit scanner les classes
  theme: {
    extend: {}, // espace pour personnaliser les couleurs, typographies, etc.
  },
  plugins: [], // aucun plugin par défaut
};
